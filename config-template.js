module.exports = {
  token: "",
  owners: [""],
  command_prefix: "!",

  // I don't recommand turning on debug unless you are doing some sort of development on the
  // bot as it could be quite spammy
  debug: false,

  // Data storage config
  guild_config_storage_location: './settings.db',
  issues_storage_location: './data.db'
};
